1. The command `command -v journalctl` checks if `journalctl` is available on the system and returns its path if found. If not, it will return nothing. This is useful for verifying if `systemd` (and its logging tool `journalctl`) is installed.


systemctl list-unit-files --type service --all



systemctl
tar
apt
systemd