apiVersion: apps/v1
kind: Deployment
metadata:
  name: demo-deploy
spec:
  replicas: 1
  selector:
    matchLabels:
      app: demo
  template:
    metadata:
      labels:
        app: demo
        color: "COLOR_PLACEHOLDER"
    spec:
      containers:
        - name: demo
          image: nginx:latest
