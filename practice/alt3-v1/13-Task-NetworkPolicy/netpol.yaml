apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: cherry-control
spec:
  podSelector:
    matchLabels:
      app: pit

  policyTypes:
    - Egress
  
  egress:
    - to:
        - podSelector:
            matchLabels:
              app: stem
