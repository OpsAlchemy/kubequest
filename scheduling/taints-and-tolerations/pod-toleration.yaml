apiVersion: v1
kind: Pod
metadata:
  name: tolerated-pod
  labels:
    app: blue
spec:
  containers:
  - name: nginx
    image: nginx
 
  tolerations:
    - key: "app"
      operator: "Equal"
      value: "blue"
      effect: "NoSchedule"
